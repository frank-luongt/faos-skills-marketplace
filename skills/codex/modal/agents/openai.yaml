# AUTO-GENERATED by export-skills.py
# Codex UI metadata mapped from FAOS skill frontmatter

interface:
  display_name: "Modal"
  short_description: "Modal serverless GPU compute platform. Use when running ML workloads on serverless GPUs with container-based..."

policy:
  allow_implicit_invocation: true

# FAOS metadata (informational)
# domain: ai-research
# priority: medium
# tags: [modal, infrastructure, gpu-cloud, serverless]
