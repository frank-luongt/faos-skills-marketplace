# AUTO-GENERATED by export-skills.py
# Codex UI metadata mapped from FAOS skill frontmatter

interface:
  display_name: "Guidance"
  short_description: "Guidance constrained generation library for LLMs. Use when enforcing structured outputs with templates, grammars, or..."

policy:
  allow_implicit_invocation: true

# FAOS metadata (informational)
# domain: ai-research
# priority: high
# tags: [guidance, prompt-engineering, structured-output]
