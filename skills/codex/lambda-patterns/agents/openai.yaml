# AUTO-GENERATED by export-skills.py
# Codex UI metadata mapped from FAOS skill frontmatter

interface:
  display_name: "Lambda Patterns"
  short_description: "AWS Lambda patterns for AI agent tool execution. Use when building Bedrock Agent action groups, serverless API backends..."

policy:
  allow_implicit_invocation: true

# FAOS metadata (informational)
# domain: cloud
# priority: high
# tags: [aws, lambda, serverless, agents]
