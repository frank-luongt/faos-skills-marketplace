# AUTO-GENERATED by export-skills.py
# Codex UI metadata mapped from FAOS skill frontmatter

interface:
  display_name: "Outlines"
  short_description: "Outlines structured text generation with guaranteed JSON/regex output. Use when enforcing structured outputs from local..."

policy:
  allow_implicit_invocation: true

# FAOS metadata (informational)
# domain: ai-research
# priority: high
# tags: [outlines, prompt-engineering, structured-output]
