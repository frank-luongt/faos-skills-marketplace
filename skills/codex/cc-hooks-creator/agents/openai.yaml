# AUTO-GENERATED by export-skills.py
# Codex UI metadata mapped from FAOS skill frontmatter

interface:
  display_name: "Cc Hooks Creator"
  short_description: "Create, configure, and debug Claude Code hooks -- shell commands that execute at lifecycle events (PreToolUse,..."

policy:
  allow_implicit_invocation: true

# FAOS metadata (informational)
# domain: tools
# priority: medium
# tags: [claude-code, hooks, automation, lifecycle]
