# AUTO-GENERATED by export-skills.py
# Codex UI metadata mapped from FAOS skill frontmatter

interface:
  display_name: "Llamaguard"
  short_description: "LlamaGuard safety classifier for LLM inputs and outputs. Use when implementing content safety filtering in production..."

policy:
  allow_implicit_invocation: true

# FAOS metadata (informational)
# domain: ai-research
# priority: high
# tags: [llamaguard, safety, alignment, content-safety]
