# AUTO-GENERATED by export-skills.py
# Codex UI metadata mapped from FAOS skill frontmatter

interface:
  display_name: "Gptq"
  short_description: "GPTQ post-training quantization for generative models. Use when quantizing large models to 4-bit with calibration-based..."

policy:
  allow_implicit_invocation: true

# FAOS metadata (informational)
# domain: ai-research
# priority: medium
# tags: [gptq, optimization, quantization]
