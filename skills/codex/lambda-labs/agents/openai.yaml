# AUTO-GENERATED by export-skills.py
# Codex UI metadata mapped from FAOS skill frontmatter

interface:
  display_name: "Lambda Labs"
  short_description: "Lambda Labs GPU cloud infrastructure. Use when provisioning on-demand GPU instances for ML training and inference..."

policy:
  allow_implicit_invocation: true

# FAOS metadata (informational)
# domain: ai-research
# priority: medium
# tags: [lambda-labs, infrastructure, gpu-cloud, gpu-cloud]
