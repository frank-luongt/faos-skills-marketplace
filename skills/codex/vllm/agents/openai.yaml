# AUTO-GENERATED by export-skills.py
# Codex UI metadata mapped from FAOS skill frontmatter

interface:
  display_name: "Vllm"
  short_description: "vLLM high-throughput LLM serving engine with PagedAttention. Use when deploying models in production with optimal GPU..."

policy:
  allow_implicit_invocation: true

# FAOS metadata (informational)
# domain: ai-research
# priority: high
# tags: [vllm, inference, serving]
