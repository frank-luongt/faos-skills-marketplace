# AUTO-GENERATED by export-skills.py
# Codex UI metadata mapped from FAOS skill frontmatter

interface:
  display_name: "Awq"
  short_description: "AWQ activation-aware weight quantization for LLMs. Use when quantizing models to 4-bit with minimal accuracy loss for..."

policy:
  allow_implicit_invocation: true

# FAOS metadata (informational)
# domain: ai-research
# priority: medium
# tags: [awq, optimization, quantization]
